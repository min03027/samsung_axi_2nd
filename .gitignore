src/main/resources/static/img/*.mp4
static/img/*.mp4

# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.vscode/
out/
bin/

# OS
Thumbs.db
.DS_Store

# 로컬 설정 (DB 비밀번호 등은 커밋 금지)
application-secret.yml
.env

# 업로드된 학습 자료 — 저장소가 아니라 서버 볼륨(도커 lxp-uploads)에 남는다.
# 커밋하면 저장소가 수십~수백 MB 로 불어나고, 훈련생 제출물이 들어갈 수도 있다.
uploads/

# Claude Code 에이전트 워크트리 (커밋 대상 아님)
.claude/worktrees/

# 에이전트 도구가 작업 중 만드는 디렉터리. src/ 와 static/ 안까지 퍼지는데,
# static/ 아래 것은 그대로 두면 jar 에 패키징돼 운영 서버로 나간다.
.omc/

# Cloudflare Wrangler local cache/state
.wrangler/
