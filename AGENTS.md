# Samsung LXP 작업 지침

이 파일은 새 Codex 세션에서도 이 저장소의 작업 방식과 사용자 선호를 동일하게 유지하기 위한 영구 지침이다. 작업을 시작할 때 가장 먼저 읽고 따른다.

## 1. 기본 원칙

- 사용자와의 대화 및 작업 보고는 한국어로 한다.
- 설명만 하지 말고, 변경 요청이면 구현·검증까지 완료한다.
- 공개 웹페이지 변경은 사용자가 `로컬만`이라고 명시하지 않는 한 Git 반영, Cloudflare 배포, 실서버 검증까지를 완료 기준으로 삼는다.
- 사용자가 제공한 스크린샷이나 레퍼런스 URL을 임의의 디자인 유행보다 우선한다.
- 기존 사용자 변경과 다른 작업자의 변경을 보존한다. 관련 없는 파일은 수정·정리·커밋하지 않는다.
- `.env`의 내용을 출력하거나 커밋하지 않는다. 비밀값은 문서, 코드, 로그에 남기지 않는다.
- `docs_v2/`는 사용자의 별도 작업물이다. 명시적인 요청이 없으면 건드리거나 커밋하지 않는다.
- 파괴적 Git 명령, 강제 푸시, 광범위한 파일 삭제를 하지 않는다.

## 2. 저장소와 배포 대상

- 저장소 루트: `/Users/min/Desktop/test/samsung-lxp`
- 기본 작업 브랜치: `main`
- 실제 서비스용 Git 원격: `cf`
- 서비스 URL: `https://samsung-axi-2nd.min0302748.workers.dev`
- 취업캠퍼스 페이지: `https://samsung-axi-2nd.min0302748.workers.dev/v2/site/campus/`
- Cloudflare 배포 명령: `npx --yes wrangler@latest deploy`

`origin`, `a` 등 다른 원격은 사용자가 명시적으로 요청한 경우에만 푸시하거나 병합한다. 여러 원격이 있다는 이유만으로 모든 원격의 작업이 병합됐다고 판단하지 않는다.

## 3. 세션 시작 절차

작업 시작 시 아래 순서로 현재 상태를 다시 확인한다. 이 문서에 과거 커밋 ID나 배포 Version ID를 고정하지 않는다.

```bash
git status --short
git branch --show-current
git log --oneline -5
git remote -v
git fetch cf
git rev-list --left-right --count main...cf/main
```

- `git status`에서 사용자의 미커밋 변경과 새 파일을 먼저 파악한다.
- `main`과 `cf/main`이 다르면 어느 쪽 변경인지 확인하고 안전하게 통합한 뒤 작업한다.
- 충돌이나 의도 불명의 변경을 임의로 버리지 않는다.

## 4. 홈페이지 변경 작업 흐름

1. `rg`, `rg --files`, `sed` 등으로 관련 HTML/CSS/JS와 현재 구현을 먼저 확인한다.
2. 필요한 경우 제공된 레퍼런스와 현재 실서비스 화면을 확인한다. 외부 페이지를 근거로 삼을 때는 현재 공식 페이지를 우선한다.
3. 변경 범위를 좁게 잡고 `apply_patch`로 수정한다.
4. CSS 또는 JS가 바뀌면 이를 참조하는 HTML의 쿼리 버전을 올려 브라우저 캐시를 무효화한다.
5. 정적 자산이 바뀌면 `src/main/cloudflare/static-assets-worker.js`의 `__axi_asset_version`도 올린다.
6. 변경 종류에 맞춰 검증한다.
7. `cf/main`의 최신 변경을 다시 확인하고, 의도한 파일만 커밋한다.
8. `git push cf main`으로 서비스 원격에 푸시한다.
9. `npx --yes wrangler@latest deploy`로 Cloudflare Workers에 배포한다.
10. 실서비스의 HTML과 변경된 CSS/JS를 직접 조회해 새 버전과 기대 문구가 반영됐는지 확인한다.
11. 완료 보고에는 공개 URL, 커밋 ID, Cloudflare Version ID, 검증 결과를 짧게 적는다.

문서나 개발 지침처럼 실제 서비스 산출물에 포함되지 않는 파일만 바꾼 경우에는 Cloudflare 배포를 생략하고 그 이유를 보고한다.

## 5. 기본 검증 명령

변경 내용에 맞는 항목만 실행하되, 최소한 `git diff --check`는 수행한다.

```bash
git diff --check
node --check path/to/changed-file.js
rg -n "확인할 문구|삭제했어야 할 문구" path/to/files
```

Java 코드가 바뀌면 프로젝트의 Gradle 설정과 Java 버전을 확인한 뒤 관련 테스트 또는 컴파일을 실행한다. 단순 프런트엔드 변경에 전체 테스트를 기계적으로 돌릴 필요는 없지만, 브라우저 구문 오류와 실서비스 반영은 반드시 확인한다.

실서비스 검증 시에는 캐시된 응답을 피하도록 변경된 자산의 최신 쿼리 버전을 사용한다. 화면 변경은 가능하면 데스크톱과 모바일 너비를 모두 확인한다.

## 6. Git 협업 규칙

- 다른 작업자가 `cf/main`에 푸시할 수 있다고 항상 가정한다.
- 커밋 직전 `git fetch cf`와 앞뒤 커밋 수를 다시 확인한다.
- `cf/main`이 앞서 있거나 분기됐으면 커밋 내용을 확인한 후 안전한 병합 또는 리베이스 방법을 선택한다.
- `git push --force`, `git reset --hard`, 사용자 파일을 되돌리는 `git checkout --`를 사용하지 않는다.
- 커밋에는 이번 요청의 파일만 넣는다. `git add .`는 피하고 경로를 명시한다.
- 커밋 메시지는 실제 변경을 나타내는 간결한 한국어 또는 프로젝트 관례에 맞는 문구로 작성한다.

## 7. 취업캠퍼스 주요 파일

- 페이지: `src/main/resources/static/v2/site/campus/index.html`
- 스타일: `src/main/resources/static/v2/assets/campus-employment.css`
- 콘텐츠 데이터: `src/main/resources/static/v2/assets/campus-content.js`
- 인터랙션: `src/main/resources/static/v2/assets/campus-employment.js`
- 정적 자산 워커: `src/main/cloudflare/static-assets-worker.js`

수정 전 실제 참조 관계를 다시 검색한다. 파일명이 같더라도 다른 페이지용 자산을 잘못 수정하지 않는다.

## 8. 취업캠퍼스 디자인 기준

- 전체 톤은 깔끔하고 신뢰감 있는 교육기관·에듀테크 서비스로 유지한다.
- AI가 자동 생성한 듯한 과장된 UI를 피한다. 불필요한 네온 글로우, 과한 그라데이션, 장식용 번호, 뻔한 선 아이콘, 의미 없는 배지와 대시보드형 카드는 사용하지 않는다.
- `검증 기준`, `VERIFIED`처럼 신뢰를 주장하기만 하는 라벨을 만들지 않는다. 신뢰는 실제 기관명, 연도, 공개 성과, 정확한 관계 문구와 자료 구조로 보여준다.
- 본문을 카드 여러 개에 길게 나열하기보다 정보 위계, 여백, 타이포그래피, 구분선과 실제 이미지로 정리한다.
- 블랙·오프화이트를 기본으로 하고 주황색은 CTA, 현재 위치, 핵심 강조에 제한적으로 사용한다. 주황색을 넓은 면적에 무조건 채우지 않는다.
- 로고는 가능하면 공식 출처의 투명 배경 자산을 사용하고 비율을 왜곡하지 않는다. 로고가 어두운 배경에서 읽히도록 필요한 최소 보정만 한다.
- 로고 롤링은 끊김 없이 계속 움직이며 마우스 오버 시 일시정지하지 않는다.
- 수상, 평가, 교육 도구 사용, 협력, 채용 관계를 서로 혼동하지 않는다. 공식 파트너십이나 채용 보장을 암시하지 않는다.
- `취업률 100%` 등 성과는 공개된 개별 과정·기수의 성과임을 정확히 쓰고 전체 과정 평균이나 향후 취업 보장으로 확대하지 않는다.
- 사용자에게 내부 작업 설명이나 변명처럼 보이는 문구를 화면에 넣지 않는다.
- 데스크톱뿐 아니라 모바일에서 잘림, 가로 스크롤, 과도한 글자 크기가 없는지 확인한다.

## 9. Google Sheets 체크리스트 관리

- 스프레드시트 ID: `1Ih76fkqIWl8U261YyrttZT7Ob2Ik1q2MKbg-lkZU2cU`
- `판정 요약` 탭 gid: `1224449170`
- `코드 대조 체크리스트` 탭 gid: `339003740`
- `빠른 완료 순서` 탭 gid: `362605538`

Google Sheets를 수정할 때는 Google Sheets 연결 기능을 사용하고 다음을 지킨다.

1. 기존 탭, 헤더, 데이터 유효성, 수식, 서식을 먼저 읽는다.
2. 정확한 행과 열을 확인한 뒤 필요한 셀 범위만 수정한다.
3. 홈페이지 기능이 바뀌면 해당 체크리스트 행의 상태와 근거를 실제 구현 수준에 맞춰 갱신한다.
4. 프런트 화면만 존재하지만 요구사항에 CMS, 서버 저장, 실제 전송이 포함되면 `구현`으로 과대 판정하지 않는다. `부분구현`과 남은 범위를 명시한다.
5. 요약 개수, 커밋 ID, 배포 정보처럼 연결된 값이 있으면 함께 갱신한다.
6. 쓰기 후 같은 범위를 다시 읽어 값과 서식이 유지됐는지 확인한다.
7. `빠른 완료 순서`는 미완료 항목을 예상 난이도와 의존성 기준으로 정렬한 보조 탭이며, 원본 체크리스트의 행 순서를 임의로 바꾸지 않는다.

## 10. 완료 보고 기준

최종 답변은 결과부터 말하고 다음 정보만 간결하게 포함한다.

- 무엇을 바꿨는지
- 어떤 검증을 통과했는지
- 커밋·푸시·배포 여부
- 사용자가 확인할 공개 URL 또는 수정한 파일 링크
- 아직 남은 제약이 있다면 정확한 이유

배포하지 않았거나 스프레드시트를 수정하지 않았다면 완료한 것처럼 말하지 않는다. 다른 사람의 변경까지 병합됐다는 주장도 실제 브랜치와 커밋을 확인한 경우에만 한다.
